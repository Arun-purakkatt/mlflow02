name: sklearn demo

conda_env: conda.yaml

entry_points:
    main:
        parameters:
            param1: {type: int,default: 0.5}
            param2: {type: int,default: 0.5}
        command: "python demo.py -a {alpha} -l1 {l1_ratio}"
